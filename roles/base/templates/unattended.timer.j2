[Unit]
Description=Daily apt upgrade and clean activities
After=apt-daily.timer

[Timer]
OnCalendar={{ base_unattended_calendar }}
RandomizedDelaySec={{ base_unattended_delay }}
Persistent=true

[Install]
WantedBy=timers.target
