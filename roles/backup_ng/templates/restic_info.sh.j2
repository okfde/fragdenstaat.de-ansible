#!/bin/bash

export TMPDIR="/var/tmp/restic"

/usr/bin/restic snapshots -r sftp:{{ backup_repo[inventory_hostname].user }}@{{ backup_repo[inventory_hostname].host }}:./{{ backup_repo[inventory_hostname].path }}/ --password-file /root/restic.passwd
echo ""
echo "-------------------------------------------------------------------------------"
/usr/bin/restic stats -r sftp:{{ backup_repo[inventory_hostname].user }}@{{ backup_repo[inventory_hostname].host }}:./{{ backup_repo[inventory_hostname].path }}/ --password-file /root/restic.passwd
