skip_list:
  - package-latest
  - git-latest
  - yaml[line-length]
  - var-naming[no-role-prefix]
  - name[casing]

warn_list:
  - yaml
  - ignore-errors

exclude_paths:
  - .pre-commit-config.yaml
  - ./playbooks/local_config.yml
