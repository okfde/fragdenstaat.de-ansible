---

- name: Icinga
  hosts: icinga
  become: true
  become_user: root
  vars_files:
    - ../local_vars/local_config.yml

  roles:
    - role: certbot
      tags: certbot
    - role: icinga
      tags: icinga
