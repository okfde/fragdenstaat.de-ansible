---

- name: Base
  hosts: qemu
  become: true
  become_user: root
  vars_files:
    - ../../local_vars/local_config.yml

  roles:
    - role: base
      tags: base
    - role: check_mk
      tags: checkmk
    - role: test/qemu
      tags: qemu
